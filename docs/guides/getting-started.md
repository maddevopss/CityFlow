# 🚀 Guide de Démarrage CityFlow

## Prérequis

- Node.js 18+
- npm 9+
- Docker & Docker Compose (optionnel)
- PostgreSQL 15+ avec PostGIS

## Installation

### Avec Docker (recommandé)

```bash
git clone https://github.com/votre-username/cityflow.git
cd cityflow
docker-compose up -d
